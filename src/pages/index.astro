---
import Layout from "@/layouts/Layout.astro";
import { getPosts } from "@/lib/notion/client";
// import { projects } from "@/data/projects";

// SSGとしてビルド時に静的生成 (Astro v5)
export const prerender = true;

const posts = await getPosts();
---

<Layout>
  {
    /*
  <!-- ===== 1. ヒーローセクション ===== -->
  <section
    class="bg-gradient-to-b from-gray-50 to-white py-20 sm:py-28 md:py-36"
  >
    <div class="max-w-3xl mx-auto px-4 text-center">
      <h1
        class="text-3xl sm:text-4xl md:text-5xl font-extrabold text-gray-900 leading-tight mb-6"
      >
        仕様が固まっていなくても、<br class="hidden sm:inline" />
        テキストベースで形にします。
      </h1>
      <p
        class="text-lg sm:text-xl text-gray-600 leading-relaxed mb-10 max-w-2xl mx-auto"
      >
        デザインの装飾よりも「速度・堅牢さ・運用のしやすさ」を優先する方のための開発パートナーです。フロントエンドからバックエンド（認証・DB・API連携）まで、実務に直結するWeb開発を一人で完結させます。
      </p>
      <a
        href="#contact"
        class="inline-flex items-center gap-2 bg-gray-900 text-white px-8 py-4 rounded-lg text-lg font-semibold hover:bg-gray-800 transition-colors shadow-lg hover:shadow-xl"
      >
        開発の相談・見積もりを依頼する
        <svg
          class="w-5 h-5"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
        </svg>
      </a>
    </div>
  </section>

  <!-- ===== 2. 提供サービス ===== -->
  <section class="py-20 sm:py-24 bg-white">
    <div class="max-w-4xl mx-auto px-4">
      <h2 class="text-2xl sm:text-3xl font-bold text-gray-900 text-center mb-4">
        提供サービス
      </h2>
      <p class="text-gray-600 text-center mb-12 max-w-xl mx-auto">
        見た目だけではない、中身で勝負するサイト・システム構築
      </p>

      <div class="grid gap-8 md:grid-cols-3">
        <!-- MVP・プロトタイプ開発 -->
        <div
          class="bg-gray-50 rounded-2xl p-8 hover:shadow-lg transition-shadow"
        >
          <div
            class="w-12 h-12 bg-blue-100 text-blue-600 rounded-xl flex items-center justify-center mb-5"
          >
            <svg
              class="w-6 h-6"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M13 10V3L4 14h7v7l9-11h-7z"></path>
            </svg>
          </div>
          <h3 class="text-xl font-bold text-gray-900 mb-3">
            MVP・プロトタイプ開発
          </h3>
          <p class="text-gray-600 leading-relaxed">
            認証（Auth）やデータベース（CRUD）を含むWebサービスの最小構成を最短で構築します。
          </p>
        </div>

        <!-- パフォーマンス最適化 -->
        <div
          class="bg-gray-50 rounded-2xl p-8 hover:shadow-lg transition-shadow"
        >
          <div
            class="w-12 h-12 bg-green-100 text-green-600 rounded-xl flex items-center justify-center mb-5"
          >
            <svg
              class="w-6 h-6"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"
              ></path>
            </svg>
          </div>
          <h3 class="text-xl font-bold text-gray-900 mb-3">
            パフォーマンス最適化
          </h3>
          <p class="text-gray-600 leading-relaxed">
            既存の重いサイトやシステムを、最新技術を用いて爆速かつモダンな環境へ再定義します。
          </p>
        </div>

        <!-- ツール・API連携開発 -->
        <div
          class="bg-gray-50 rounded-2xl p-8 hover:shadow-lg transition-shadow"
        >
          <div
            class="w-12 h-12 bg-purple-100 text-purple-600 rounded-xl flex items-center justify-center mb-5"
          >
            <svg
              class="w-6 h-6"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M11 4a2 2 0 114 0v1a1 1 0 001 1h3a1 1 0 011 1v3a1 1 0 01-1 1h-1a2 2 0 100 4h1a1 1 0 011 1v3a1 1 0 01-1 1h-3a1 1 0 01-1-1v-1a2 2 0 10-4 0v1a1 1 0 01-1 1H7a1 1 0 01-1-1v-3a1 1 0 00-1-1H4a2 2 0 110-4h1a1 1 0 001-1V7a1 1 0 011-1h3a1 1 0 001-1V4z"
              ></path>
            </svg>
          </div>
          <h3 class="text-xl font-bold text-gray-900 mb-3">
            ツール・API連携開発
          </h3>
          <p class="text-gray-600 leading-relaxed">
            外部サービスとのAPI連携や、特定の業務を自動化・効率化する専用ツールの作成を行います。
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== 3. 実績紹介 ===== -->
  <section class="py-20 sm:py-24 bg-gray-50">
    <div class="max-w-4xl mx-auto px-4">
      <div class="text-center mb-12">
        <h2 class="text-2xl sm:text-3xl font-bold text-gray-900 mb-4">
          実績紹介
        </h2>
        <p class="text-gray-600 max-w-xl mx-auto">
          技術スタックを基盤とした実装例
        </p>
      </div>

      <div class="grid gap-8 md:grid-cols-2 mb-10">
        {
          projects.slice(0, 2).map((project) => (
            <a
              href={project.url}
              target="_blank"
              rel="noopener noreferrer"
              class={`block bg-white rounded-2xl p-8 shadow-sm hover:shadow-lg transition-shadow border border-gray-100 hover:border-${project.colorTheme}-200`}
            >
              <span
                class={`inline-block px-3 py-1 bg-${project.colorTheme}-100 text-${project.colorTheme}-700 text-xs font-medium rounded-full mb-4`}
              >
                {project.tags[0]} / {project.tags[1]}
              </span>
              <h3 class="text-xl font-bold text-gray-900 mb-3">
                {project.title}
              </h3>
              <p class="text-sm text-gray-500 mb-4">{project.subtitle}</p>
              <p class="text-gray-600 leading-relaxed">{project.description}</p>
            </a>
          ))
        }
      </div>

      <div class="text-center">
        <a
          href="/portfolio"
          class="inline-flex items-center gap-2 text-gray-900 font-semibold hover:text-blue-600 transition-colors"
        >
          すべての実績を見る
          <svg
            class="w-4 h-4"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
          </svg>
        </a>
      </div>
    </div>
  </section>

  <!-- ===== 4. 開発ポリシー ===== -->
  <section class="py-20 sm:py-24 bg-white">
    <div class="max-w-3xl mx-auto px-4 text-center">
      <h2 class="text-2xl sm:text-3xl font-bold text-gray-900 mb-6">
        開発ポリシー
      </h2>
      <blockquote
        class="text-xl sm:text-2xl font-medium text-gray-700 italic mb-8"
      >
        「コードの美しさと表示速度こそが、最高のユーザー体験」
      </blockquote>
      <p class="text-gray-600 leading-relaxed mb-6">
        派手な装飾に工数をかける代わりに、SEO（GEO）やストレスのない表示速度にリソースを集中させます。
      </p>
      <p class="text-sm text-gray-500">
        ※ デザインは標準的なUIキット（Tailwind
        UI等）を使用します。これにより、デザイン工程のコストを抑え、機能実装と速度に全振りの開発を可能にしています。
      </p>
    </div>
  </section>

  <!-- ===== 5. お問い合わせ ===== -->
  <section
    id="contact"
    class="py-20 sm:py-24 bg-gray-900 text-white scroll-mt-20"
  >
    <div class="max-w-2xl mx-auto px-4 text-center">
      <h2 class="text-2xl sm:text-3xl font-bold mb-4">開発のご相談</h2>
      <p class="text-gray-400 mb-12 max-w-xl mx-auto">
        「こんなツールが欲しい」「サイトを速くしてほしい」といった、ざっくりとしたご要望で構いません。テキストベースでの柔軟な要件定義を得意としていますので、お気軽にご連絡ください。
      </p>

      <a
        href="https://docs.google.com/forms/d/e/1FAIpQLSdVuRACuWh4Bv1IH1CPwrzIRTMdY6Zhx_2IsWbgMCjYdl8RcQ/viewform?usp=dialog"
        target="_blank"
        rel="noopener noreferrer"
        class="inline-flex items-center gap-2 bg-white text-gray-900 px-8 py-4 rounded-lg text-lg font-semibold hover:bg-gray-100 transition-colors shadow-lg hover:shadow-xl"
      >
        相談フォームを開く
        <svg
          class="w-5 h-5"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"
          ></path>
        </svg>
      </a>
    </div>
  </section>
  */
  }

  <!-- ===== 6. 最新記事一覧 ===== -->
  <section class="py-20 sm:py-24 bg-gray-50">
    <div class="max-w-5xl mx-auto px-4">
      <h2 class="text-2xl sm:text-3xl font-bold text-gray-900 text-center mb-4">
        最新記事
      </h2>
      <p class="text-gray-600 text-center mb-12 max-w-xl mx-auto">
        少しずつでも、確実に成長していく場所
      </p>

      {
        posts.length === 0 ? (
          <div class="text-center py-16 px-4">
            <div class="inline-flex items-center justify-center w-16 h-16 bg-gray-100 rounded-full mb-4">
              <svg
                class="w-8 h-8 text-gray-400"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"
                />
              </svg>
            </div>
            <p class="text-gray-600 text-lg font-medium mb-2">
              まだ公開中の記事はありません。
            </p>
            <p class="text-gray-500 text-sm">
              Notionデータベースを確認し、Status = "Published"
              の記事を追加してください。
            </p>
          </div>
        ) : (
          <div class="grid gap-6 md:gap-8 md:grid-cols-2 lg:grid-cols-2">
            {posts.map((post, index) => (
              <article class="group bg-white rounded-2xl overflow-hidden shadow-sm hover:shadow-xl border border-gray-100 hover:border-gray-200 transition-all duration-300">
                <a href={`/${post.slug}`} class="block">
                  {post.coverImage && (
                    <div class="aspect-[16/9] overflow-hidden bg-gray-100 relative">
                      <img
                        src={post.coverImage}
                        alt={post.title}
                        class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
                        loading={index < 2 ? "eager" : "lazy"}
                        decoding="async"
                        width="800"
                        height="450"
                      />
                      <div class="absolute inset-0 bg-gradient-to-t from-black/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300" />
                    </div>
                  )}
                  <div class="p-6">
                    <div class="flex items-center gap-3 mb-3 text-xs text-gray-500">
                      {post.publishedDate && (
                        <time
                          datetime={post.publishedDate}
                          class="flex items-center gap-1.5"
                        >
                          <svg
                            class="w-3.5 h-3.5"
                            fill="none"
                            stroke="currentColor"
                            viewBox="0 0 24 24"
                          >
                            <path
                              stroke-linecap="round"
                              stroke-linejoin="round"
                              stroke-width="2"
                              d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"
                            />
                          </svg>
                          {new Date(post.publishedDate).toLocaleDateString(
                            "ja-JP",
                            {
                              year: "numeric",
                              month: "short",
                              day: "numeric",
                            },
                          )}
                        </time>
                      )}
                      {post.author && (
                        <>
                          <span class="text-gray-400" aria-hidden="true">
                            •
                          </span>
                          <span class="flex items-center gap-1.5">
                            <svg
                              class="w-3.5 h-3.5"
                              fill="none"
                              stroke="currentColor"
                              viewBox="0 0 24 24"
                            >
                              <path
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                stroke-width="2"
                                d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"
                              />
                            </svg>
                            {post.author}
                          </span>
                        </>
                      )}
                    </div>

                    <h3 class="text-xl sm:text-2xl font-bold mb-3 text-gray-900 group-hover:text-blue-600 transition-colors line-clamp-2">
                      {post.title}
                    </h3>

                    {post.excerpt && (
                      <p class="text-gray-600 text-sm leading-relaxed mb-4 line-clamp-3">
                        {post.excerpt}
                      </p>
                    )}
                  </div>
                </a>
              </article>
            ))}
          </div>
        )
      }
    </div>
  </section>
</Layout>
